function checkNumberRange(n) {
    if (n >= 100 && n <= 200) {
      console.log("The number is in the range");
    } else {
      console.log("Number is not in range");
    }
  }
  
  // Example usage: calling the function with a number
  checkNumberRange(150);
  